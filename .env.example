# ─── LOCAL (docker-compose) ───────────────────────────────────────────────────
# DATABASE_URL=postgresql://jero:123@localhost:5433/geography_db

# ─── DEVELOP / TEST (Neon) ────────────────────────────────────────────────────
# DATABASE_URL=postgresql://neondb_owner:npg_Fbusz3x7vSWY@ep-cool-lab-ait9eawb-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# ─── PRODUCTION (Neon) ────────────────────────────────────────────────────────
# DATABASE_URL=postgresql://neondb_owner:npg_bFYDynow1IU2@ep-noisy-base-aiqfltq0-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# ─── GCP / Cloud SQL ──────────────────────────────────────────────────────────
# DATABASE_URL=postgresql://jero:Geo1234!@34.10.221.217:5432/geography_db


# ══════════════════════════════════════════════════════════════════════════════
# GCP INFRASTRUCTURE REFERENCE
# ══════════════════════════════════════════════════════════════════════════════
#
# Project ID:        geography-api-493315
# Project Number:    492692263761
# Region:            us-central1
#
# ── Cloud SQL ─────────────────────────────────────────────────────────────────
# Instance:          geography-db
# Engine:            PostgreSQL 13  (db-f1-micro)
# Public IP:         34.10.221.217
# Port:              5432
# Database:          geography_db
# User:              jero
# Password:          Geo1234!
# Authorized nets:   0.0.0.0/0
#
# ── GKE ───────────────────────────────────────────────────────────────────────
# Cluster:           geography-cluster
# Nodes:             3 x e2-medium
# K8s version:       1.35.1
# Service IP:        35.194.53.58  (LoadBalancer, port 80 → 3000)
# Namespace:         geography-api
# K8s secret:        geography-api-secret  (key: DATABASE_URL)
#
# ── Artifact Registry ─────────────────────────────────────────────────────────
# Repo:              geography-api
# Region:            us-central1
# Full path:         us-central1-docker.pkg.dev/geography-api-493315/geography-api
#
# ── Cloud Build ───────────────────────────────────────────────────────────────
# Service account:   492692263761@cloudbuild.gserviceaccount.com
# Trigger:           push to ^master$ → cloudbuild.yaml
# GitHub repo:       cubito1080/devops
#
# ── USEFUL COMMANDS ───────────────────────────────────────────────────────────
# Connect to cluster:
#   gcloud container clusters get-credentials geography-cluster --region=us-central1 --project=geography-api-493315
#
# Recreate K8s secret (if cluster was deleted):
#   kubectl create namespace geography-api
#   kubectl create secret generic geography-api-secret --namespace=geography-api \
#     --from-literal=DATABASE_URL='postgresql://jero:Geo1234!@34.10.221.217:5432/geography_db'
#
# Seed the database locally (with dotenv already patched):
#   printf 'DATABASE_URL=postgresql://jero:Geo1234!@34.10.221.217:5432/geography_db\nNODE_ENV=production\nPORT=3000\n' > .env \
#   && npm run seed \
#   && cp .env.backup .env
#
# Trigger a redeploy:
#   git commit --allow-empty -m ':rocket: chore: redeploy' && git push origin master
#
# ══════════════════════════════════════════════════════════════════════════════


DATABASE_URL=postgresql://user:password@localhost:5432/dbname
NODE_ENV=development
PORT=3000
