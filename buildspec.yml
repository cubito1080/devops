# ⚠️  DEPRECATED — This project has migrated from AWS to Google Cloud Platform.
#
# This file (AWS CodeBuild buildspec) is no longer used.
# The active CI/CD pipeline configuration is:
#
#   cloudbuild.yaml  ← Cloud Build (GCP equivalent of CodeBuild)
#
# You can safely delete this file once the GCP pipeline is confirmed working.
