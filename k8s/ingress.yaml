# Ingress is NOT used in this project.
#
# Traffic reaches the API through the LoadBalancer Service (service.yaml),
# which GKE provisions with a public IP automatically — no Ingress controller
# or HTTP(S) Load Balancer setup required.
#
# This file is kept as a placeholder. Do not apply it.
